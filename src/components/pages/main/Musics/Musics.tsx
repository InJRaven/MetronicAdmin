import { PageHeading } from "../../../ui";

const Musics = () => {
  return (
    <PageHeading
      title="Musics Page"
      description="Curate and control your music library."
    />
  );
};

export default Musics;
